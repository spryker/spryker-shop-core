{% extends model('component') %}

{% define config = {
    name: 'validate-next-checkout-step',
    tag: 'validate-next-checkout-step',
} %}

{% define attributes = {
    'trigger-selector': required,
    'target-selector': required,
    'shipping-address-toggler-selector': required,
    'toggle-option-value': '-1',
} %}
