{% extends template('widget') %}

{% define data = {
    quote: _widget.quote,
} %}

{% block body %}
    <a href="{{ url('shopping-list/create-from-exist-cart', {'idQuote': (data.quote.idQuote)}) }}">
        {{ 'widget.shopping_list.multi_cart.to_shopping_list' | trans }}
    </a>
{% endblock %}
