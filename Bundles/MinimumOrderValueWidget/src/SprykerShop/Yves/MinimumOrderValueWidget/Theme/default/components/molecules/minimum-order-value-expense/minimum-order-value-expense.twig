{% extends model('component') %}

{% define config = {
    name: 'minimum-order-value-expense',
    tag: 'li'
} %}

{% define data = {
    expense: required
} %}

{% block body %}
    <div class="grid grid--justify">
        <div class="col col--sm-8">{{ 'minimum-order-value.expense.name' | trans }}</div>
        <div class="col col--sm-4 text-right"> {{ data.expense.sumPrice | money }}</div>
    </div>
{% endblock %}
