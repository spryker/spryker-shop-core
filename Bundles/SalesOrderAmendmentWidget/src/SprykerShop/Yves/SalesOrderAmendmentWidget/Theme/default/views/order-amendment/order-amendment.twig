{# @var order \Generated\Shared\Transfer\OrderTransfer #}

{% extends template('widget') %}

{% define data = {
    form: _widget.form,
    order: _widget.order,
    hasConfirmationPopup: _widget.isOrderAmendmentConfirmationEnabled,
} %}

{% block body %}
    {% include molecule('order-amendment', 'SalesOrderAmendmentWidget') with {
        data: {
            form: data.form,
            order: data.order,
            hasConfirmationPopup: data.hasConfirmationPopup,
        },
    } only %}
{% endblock %}
