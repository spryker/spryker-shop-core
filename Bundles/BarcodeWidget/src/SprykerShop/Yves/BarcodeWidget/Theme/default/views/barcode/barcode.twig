{% extends template('widget') %}

{% define data = {
    code: _widget.barcodeResponse.getCode,
    encoding: _widget.barcodeResponse.getEncoding,
} %}

{% set imageClass = '' %}

{% block body %}
    <img src="{{ data.encoding }},{{ data.code }}" class="{{ imageClass }}" alt="{{ data.code }}">
{% endblock %}
