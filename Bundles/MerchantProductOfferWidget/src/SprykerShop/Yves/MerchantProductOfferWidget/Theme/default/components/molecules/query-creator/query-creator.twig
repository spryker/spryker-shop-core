{% extends model('component') %}

{% define config = {
    name: 'query-creator',
    tag: 'query-creator',
} %}

{% define attributes = {
    'trigger-class': required,
    event: 'change',
    query: required,
} %}

{%- block class -%}
    {{ parent() }} {{ config.jsName }}
{%- endblock -%}
