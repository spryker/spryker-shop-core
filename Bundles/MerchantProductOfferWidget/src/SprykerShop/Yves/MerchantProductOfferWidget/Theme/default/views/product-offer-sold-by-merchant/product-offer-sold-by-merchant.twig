{% extends template('widget') %}

{% define data = {
    merchant: _widget.merchant | default,
    locale: app.locale,
} %}

{% block template %}
    {% if data.merchant %}
        {% set merchantUrl = null %}

        {% for urlTransfer in  data.merchant.urlCollection %}
            {% if urlTransfer.localeName == data.locale %}
                {% set merchantUrl = urlTransfer.url %}
            {% endif %}
        {% endfor %}

        <p>
            {{ 'merchant_product_offer.sold_by' | trans }} <a href="{{ merchantUrl }}" target="_blank" >{{ data.merchant.name }}</a>
        </p>
    {% endif %}
{% endblock %}
