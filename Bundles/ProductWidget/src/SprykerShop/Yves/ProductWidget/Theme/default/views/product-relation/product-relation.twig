{% include molecule('product-card') with {
    modifiers: ['stretch'],
    data: {
        name: _widget.product.name,
        abstractId: _widget.product.idProductAbstract,
        url: _widget.product.url,
        imageUrl: _widget.product.images[0].externalUrlSmall | default(null),
        price: _widget.product.price,
        originalPrice: _widget.product.prices['ORIGINAL'] | default(null)
    }
} only %}
