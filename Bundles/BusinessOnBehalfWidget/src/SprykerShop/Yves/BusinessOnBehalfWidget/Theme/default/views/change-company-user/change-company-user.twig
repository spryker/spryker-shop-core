{% extends template('widget') %}

{% define data = {
    isVisible: _widget.isVisible,
    isCustomerChangeAllowed: _widget.isCustomerChangeAllowed,
} %}

{% block template %}
    {% if data.isVisible and data.isCustomerChangeAllowed %}
        {{parent()}}
    {% endif %}
{% endblock %}

{% block body %}
    <a href="{{ path('company/user/select') }}" data-id="change-company-user">{{ "business_on_behalf_widget.change_company_user" | trans }}</a>
{% endblock %}
