{% extends model('component') %}

{% set config = {
    name: 'test-component'
} | merge(config | default({})) %}

{% block body %}
    This is an <strong class="{{config.selector}}-something">atom</strong> component.
{% endblock %}

