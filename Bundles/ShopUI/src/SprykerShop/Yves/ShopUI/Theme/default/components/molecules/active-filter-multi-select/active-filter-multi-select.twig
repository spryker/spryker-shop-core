{% extends molecule('active-filter-enumeration') %}

{% set config = {
    name: 'active-filter-multi-select'
} | merge(config | default({})) %}

{% set data = data {
    isMultiSelect: true
} | merge(data | default({})) %}
