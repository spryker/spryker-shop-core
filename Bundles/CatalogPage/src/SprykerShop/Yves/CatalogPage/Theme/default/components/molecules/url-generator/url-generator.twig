{% extends model('component') %}

{% define config = {
    name: 'url-generator',
    tag: 'url-generator',
} %}

{% define attributes = {
    'form-class-name': required,
    'trigger-class-name': required,
} %}
