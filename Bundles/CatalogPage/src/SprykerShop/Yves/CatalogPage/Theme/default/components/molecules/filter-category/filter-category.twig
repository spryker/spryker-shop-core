{% extends model('component') %}

{% define config = {
    name: 'filter-category',
} %}

{% block body %}
    {% if app.twig.function('prepareFilterCategories') %}
        {% include molecule('filter-category-prepared', 'CatalogPage') %}
    {% else %}
        {% include molecule('filter-category-bc', 'CatalogPage') %}
    {% endif %}
{% endblock %}
