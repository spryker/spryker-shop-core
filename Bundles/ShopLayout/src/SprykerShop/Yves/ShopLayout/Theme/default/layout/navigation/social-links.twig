{% for node in _widget.navigationTree.nodes %}
    {% embed '@ShopLayout/layout/navigation/_partials/base-node.twig' %}
        {% block nodeContainer %}
            <div class="picture">
                {{ parent() }}
            </div>
        {% endblock %}

        {% block url %}
            <a class="picture__icon {{ class }}" href="{{ url }}" title="{{ title }}"></a>
        {% endblock %}

        {% block externalUrl %}
            <a class="picture__icon {{ class }}" href="{{ url }}" target="_blank" title="{{ title }}"></a>
        {% endblock %}

        {% block other %}
            <span class="picture__icon {{ class }}" title="{{ title }}"></span>
        {% endblock %}
    {% endembed %}
{% endfor %}
