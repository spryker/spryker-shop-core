{% set nodes = nodes | default(_widget.navigationTree.nodes) %}

{% include molecule('navigation-multilevel') with {
    data: {
        nodes: nodes,
        menuInline: false,
        menuDropdown: false
    }
} only %}
