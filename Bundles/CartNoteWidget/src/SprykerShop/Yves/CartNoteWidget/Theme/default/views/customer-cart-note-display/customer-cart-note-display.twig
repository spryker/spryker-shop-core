{% extends template('widget') %}

{% define data = {
    label: 'cart_note.customer_page.order_note',
    note: _widget.order.cartNote
} %}

{% block body %}
    {% if data.note is not empty %}
        {% include molecule('note-list', 'CartNoteWidget') with {
            data: {
                label: data.label,
                note: data.note
            }
        } only %}
    {% endif %}
{% endblock %}
