{% extends template('widget') %}

{% define data = {
    label: '',
    note: null
} %}

{% block body %}
    {% if data.note is not empty %}
        <ul>
            <li><small>{{ data.label | trans }}:</small></li>
            <li>{{ data.note | escape | nl2br }}</li>
        </ul>
    {% endif %}
{% endblock %}
