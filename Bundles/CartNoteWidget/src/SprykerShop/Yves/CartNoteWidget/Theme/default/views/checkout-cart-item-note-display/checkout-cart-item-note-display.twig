{% extends template('widget') %}

{% define data = {
    label: 'cart_note.checkout_page.item_note',
    note: _widget.item.cartNote
} %}

{% block body %}
    {% if data.note is not empty %}
        {% include molecule('note-list', 'CartNoteWidget') with {
            data: {
                label: data.label,
                note: data.note
            }
        } only %}
    {% endif %}
{% endblock %}
