{% if _widget.quote.cartNote is not null %}
<div class="callout">
    <small>{{ 'cart_note.checkout_page.quote_note' | trans }}:</small>
    <br>
    <span>{{ _widget.quote.cartNote | escape | nl2br }}</span>
</div>
{% endif %}
