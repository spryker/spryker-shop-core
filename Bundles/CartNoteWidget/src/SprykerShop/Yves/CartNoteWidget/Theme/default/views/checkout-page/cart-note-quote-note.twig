{% extends template('widget') %}

{% define data = {
    quote: _widget.quote
} %}

{% block body %}
    {% if data.quote.cartNote is not null %}
<div class="box">
    <div class="callout">
        <small>{{ 'cart_note.checkout_page.quote_note' | trans }}:</small>
        <br>
        <span>{{ data.quote.cartNote | escape | nl2br }}</span>
    </div>
</div>
    {% endif %}
{% endblock %}
