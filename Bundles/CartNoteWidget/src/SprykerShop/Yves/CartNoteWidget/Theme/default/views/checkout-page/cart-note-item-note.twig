{% if _widget.item.cartNote is not null %}
    <br />
    <small>{{ 'cart_note.checkout_page.item_note' | trans }}:</small>
    <br />
    <span>{{ _widget.item.cartNote | escape | nl2br }}</span>
{% endif %}
