{% extends template('widget') %}

{% define data = {
    form: _widget.cartNoteForm,
    formAction: url('cart-note/quote')
} %}

{% block body %}
    {% include molecule('form') with {
        data: {
            form: data.form,
            options: {
                action: data.formAction
            },
            submit: {
                enable: true,
                text: 'cart_note.form.save' | trans
            }
        }
    } only %}
{% endblock %}
