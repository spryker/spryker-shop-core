{% extends model('component') %}

{% define config = {
    name: 'navigation-relocator',
    tag: 'navigation-relocator',
} %}

{% define attributes = {
    'block-to-move-class-name': required,
    'block-to-place-class-name': required,
    'toggle-list-classes': 'menu',
    'toggle-list-item-classes': 'navigation-multilevel-node menu__item menu__item--has-children-list',
    breakpoint: 769,
    'debounce-delay': 500,
} %}
