{% extends model('component') %}

{% define config = {
    name: 'form-value-setter',
    tag: 'form-value-setter',
} %}

{% define attributes = {
    'form-class-name': required,
    'trigger-class-name': required,
    'target-class-name': required,
    'value-field': 'set-value',
} %}
