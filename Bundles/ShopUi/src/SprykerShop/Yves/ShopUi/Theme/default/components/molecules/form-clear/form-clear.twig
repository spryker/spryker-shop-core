{% extends model('component') %}

{% define config = {
    name: 'form-clear',
    tag: 'form-clear',
} %}

{# @deprecated properties "form-selector", "trigger-selector", "ignore-selector" are deprecated:
use "form-class-name", "trigger-class-name", "ignore-class-name" instead. #}
{% define attributes = {
    'form-selector': '',
    'form-class-name': '',
    'trigger-selector': '',
    'trigger-class-name': '',
    'ignore-selector': '',
    'ignore-class-name': '',
} %}

{% block class %}{{ parent() }} {{ config.jsName }}{% endblock %}
