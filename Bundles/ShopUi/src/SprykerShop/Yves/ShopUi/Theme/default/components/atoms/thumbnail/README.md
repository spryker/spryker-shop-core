# thumbnail (atom)

Creates a simple image element.

## Code sample

```
{% include atom('thumbnail') with {
    attributes: {
        alt: 'alt',
        src: src,
        title: 'title'
    }
} only %}
```
