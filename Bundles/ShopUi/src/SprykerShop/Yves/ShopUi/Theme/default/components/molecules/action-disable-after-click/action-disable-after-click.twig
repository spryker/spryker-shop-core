{% extends model('component') %}

{% define config = {
    name: 'action-disable-after-click',
    tag: 'action-disable-after-click'
} %}

{% define attributes = {
    'disable-selector': required
} %}
