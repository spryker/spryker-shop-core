{% extends molecule('simple-carousel') %}

{% define config = {
    name: 'image-carousel',
    tag: 'image-carousel',
} %}

{% block extraClass %}
    {{ config.jsName }}
{% endblock %}

{% block component %}
    {% if data.slides %}
        {{ parent() }}
    {% endif %}
{% endblock %}

{% block slide %}
    {% include molecule('lazy-image') with {
        modifiers: ['thumbnail', 'bigger'],
        data: {
            imageSrc: slide.externalUrlLarge,
            imageTitle: slide.altText | default,
            extraAttributes: {
                itemprop: 'image',
            },
        },
    } only %}
{% endblock %}
