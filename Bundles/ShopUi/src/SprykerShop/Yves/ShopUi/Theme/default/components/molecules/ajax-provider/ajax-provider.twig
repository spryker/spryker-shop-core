{% extends model('component') %}

{% define config = {
    name: 'ajax-provider',
    tag: 'ajax-provider'
} %}

{% define attributes = {
    'fetch-on-load': false,
    'url': required,
    'method': 'get',
    'response-type': 'text',
    'event-revealer': null,
    'event-host': null,
    'element-visibility-checker-class-name': null,
    'dynamic-params': '',
} %}
