# price-mode (molecule)

Displays a simple label with an icon.

## Code sample

```
{% include molecule('price-mode') with {
    data: {
        priceMode: data.priceMode,
        showIcon: false
    }
} only %}
```
