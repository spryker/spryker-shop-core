{% extends model('component') %}

{% define config = {
    name: 'toggler-clicker',
    tag: 'toggler-clicker'
} %}

{% define attributes = {
    'trigger-selector': required,
    'target-selector': required,
    'class-to-toggle': required
} %}
