{% extends model('component') %}

{% define config = {
    name: 'icon',
    tag: 'svg',
} %}

{% define data = {
    name: required,
} %}

{% define attributes = {
    title: data.name,
} %}

{% block body %}
    <use xlink:href="#:{{ data.name }}"></use>
{% endblock %}
