# jumbotron (molecule)

Displays banner with image and short description.

## Code sample

```
{% include molecule('jumbotron') with {
    data: {
        imageUrl: 'imageUrl',
        bgColor: 'bgColor',
        textColor: 'textColor'
    }
} only %}
```
