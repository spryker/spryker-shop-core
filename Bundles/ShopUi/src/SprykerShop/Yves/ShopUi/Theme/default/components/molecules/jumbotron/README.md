Displays a banner with an image and a short description.

## Code sample

```
{% include molecule('jumbotron') with {
    data: {
        imageUrl: 'imageUrl',
        bgColor: 'bgColor',
        textColor: 'textColor'
    }
} only %}
```
