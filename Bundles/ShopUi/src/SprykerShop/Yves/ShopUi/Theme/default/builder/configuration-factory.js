module.exports = class ConfigurationFactory {
    constructor(settings, finder) {
        this.settings = settings;
        this.finder = finder;
    }

    createConfiguration() {
        /**
         *
         * configuration
         */
        return {}
    }
}
