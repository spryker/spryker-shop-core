{% extends model('component') %}

{% define config = {
    name: 'thumbnail',
    tag: 'span',
} %}

{% block attributes %}{% endblock %}

{% block body %}
    <img class="{{ config.name }}__image {% block additionalClass %}{% endblock %}" {{ component.renderAttributes(attributes) }}>
{% endblock %}
