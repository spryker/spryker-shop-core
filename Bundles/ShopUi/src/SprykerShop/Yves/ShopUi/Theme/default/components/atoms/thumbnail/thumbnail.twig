{% extends model('component') %}
 
{% set config = {
    name: 'thumbnail',
    tag: 'span'
} | merge(config | default({})) %}

{% block attributes %}{% endblock %}

{% block body %}
    <img class="{{config.name}}__image" {{block('renderAttributes')}} />
{% endblock %}

