{% extends template('product-review-list', 'ProductReviewWidget') %}

{% define data = {
    reviews: required,
    summary: required,
    ratingMaxValue: required,
    hasCustomer: required,
    pagination: required
} %}

{% block body %}
    {{ parent() }}
{% endblock %}
