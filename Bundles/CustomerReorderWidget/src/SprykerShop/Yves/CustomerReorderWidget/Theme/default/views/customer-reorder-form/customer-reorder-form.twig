{% extends template('widget') %}

{% define data = {
    form: _widget.form,
    order: _widget.order,
} %}

{% block body %}
    {{ form_start(data.form, {'action': path('customer/order/reorder', {'idSalesOrder': data.order.idSalesOrder})}) }}
        {% block embeddedData %}
            <button class="button">{{ 'customer.order.reorder_all' | trans }}</button>
        {% endblock %}
    {{ form_end(data.form) }}
{% endblock %}
