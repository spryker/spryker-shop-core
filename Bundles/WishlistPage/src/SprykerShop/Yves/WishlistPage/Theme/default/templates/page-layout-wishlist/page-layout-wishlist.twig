{% extends template('page-layout-customer', 'CustomerPage') %}

{% define data = {
    wishlists: required,
    title: 'customer.account.wishlist' | trans,
    activePage: 'wishlist',
    activeWishlistId: null
} %}

{% block customerNavigation %}
    {% widget 'CustomerNavigationWidget' args [data.activePage, data.activeWishlistId] only %}
    {% elsewidget 'CustomerNavigationWidgetPlugin' args [data.activePage, data.activeWishlistId] only %} {# @deprecated Use CustomerNavigationWidget instead. #}
    {% endwidget %}
{% endblock %}
