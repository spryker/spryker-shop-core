{% extends template('widget') %}

{% define data = {
    order: _widget.order,
    isVisible: _widget.isVisible,
} %}

{% block body %}
    {% if data.isVisible %}
        {% block content %}
            {{ data.order.firstName ~ ' ' ~ data.order.lastname }}
        {% endblock %}
    {% endif %}
{% endblock %}
