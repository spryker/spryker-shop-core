{{ form_start(_widget.voucherForm) }}
<div class="callout">
    <div class="row">
        <div class="small-12 columns">
            <h5>{{ _widget.voucherForm.voucher_code.vars.label | trans }}</h5>
            <div class="input-group">
                {{ form_widget(_widget.voucherForm.voucher_code, {attr: {class: 'input-group-field'}}) }}
                <div class="input-group-button">
                    <button class="button expanded info" type="submit"><i class="fa fa-gift"></i>{{ 'cart.voucher.redeem' | trans }}</button>
                </div>
            </div>
            {{ form_errors(_widget.voucherForm.voucher_code) }}
        </div>
    </div>
</div>
{{ form_end(_widget.voucherForm) }}
