{% extends template('page-layout-multi-cart', 'MultiCartPage') %}

{% define data = {
    form: _view.quoteForm,
    formAction: url('multi-cart/create'),
    title: 'multi_cart.form.create_cart' | trans,
} %}

{% block breadcrumbs %}
    {% include molecule('breadcrumb') with {
        data: {
            steps: [
                {
                    label: 'customer.account' | trans,
                    url: url('customer/overview'),
                },
                {
                    label: 'page.multi_cart.shopping_cart.list.title' | trans,
                    url: url('multi-cart'),
                },
                {
                    label: 'page.multi_cart.shopping_cart.create.title' | trans,
                }
            ]
        }
    } only %}
{% endblock %}

{% block customerContent %}
    <div class="box">
        {% include molecule('form') with {
            data: {
                title: 'page.multi_cart.shopping_cart.create.title' | trans,
                form: data.form,
                options: {
                    action: data.formAction
                },
                submit: {
                    enable: true
                },
                cancel: {
                    enable: true,
                    url: data.backUrl,
                    text: 'general.back.button' | trans
                }
            }
        } only %}
    </div>
{% endblock %}
