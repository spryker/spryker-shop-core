{% extends model('component') %}

{% set config = {
    name: 'tag',
    tag: 'span'
} | merge(config | default({})) %}

{% set data = {
    text: data.text | default('')
} | merge(data | default({})) %}

{% block body %}
    <span class="{{config.name}}__text">{{data.text}}</span>
{% endblock %}
