{% extends model('component') %}

{% define config = {
    name: 'page-location-search-params-updater',
    tag: 'page-location-search-params-updater',
} %}

{% define attributes = {
    'trigger-class': required,
    event: 'change',
    'search-params': required,
} %}

{%- block class -%}
    {{ parent() }} {{ config.jsName }}
{%- endblock -%}
