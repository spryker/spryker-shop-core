{% extends template('page-layout-main') %}

{% define data = {
    title: 'configurable_bundle_page.configurator' | trans,
    configurableBundleTemplates: _view.configurableBundleTemplates,
} %}

{% block content %}
    {% include molecule('templates-list', 'ConfigurableBundlePage') with {
        data: {
            templates: data.configurableBundleTemplates,
        },
    } only %}
{% endblock %}
