{% extends template('page-layout-main') %}

{% define data = {
    title: 'configurable_bundle_page.configurator' | trans,
    configurableBundleTemplates: _view.configurableBundleTemplates
} %}

{% block content %}

    {% for configurableBundleTemplate in data.configurableBundleTemplates %}
        <pre>
            {{ dump(configurableBundleTemplate.toArray()) }}
        </pre>

        <hr/>
    {% endfor %}

{% endblock %}
