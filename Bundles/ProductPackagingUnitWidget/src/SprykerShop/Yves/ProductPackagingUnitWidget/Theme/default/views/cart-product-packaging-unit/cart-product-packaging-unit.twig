{% extends template('widget') %}

{% define data = {
    cartItem: _widget.itemTransfer
} %}

{% block body %}
    {% include molecule('cart-product-packaging-unit', 'ProductPackagingUnitWidget') with {
        data: {
            cartItem: data.cartItem
        }
    } only %}
{% endblock body %}