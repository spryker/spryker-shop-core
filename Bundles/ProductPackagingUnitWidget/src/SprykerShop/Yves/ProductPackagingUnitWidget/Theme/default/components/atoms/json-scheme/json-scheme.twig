{# fe-sniffer:disabled correct-twig-component-structure #}
{% extends model('component') %}

{% define config = {
    name: data.name ~ '__json',
} %}

{% define data = {
    name: required,
    json: required,
} %}

{% define attributes = {
    json: data.json,
} %}
