{% extends model('component') %}

{% define config = {
    name: 'agent-navigation',
    tag: 'div'
} %}

{% define data = {
    agent: required,
    nodes: [
        {
            title: 'agent.account.page_title' | trans ~ ': ' ~ data.agent.firstName ~ ' ' ~ data.agent.lastName,
            url: null,
            nodeType: 'label',
            cssClass: 'menu__item--revert',
            isActive: true,
            validFrom: null,
            validTo: null,
            children: [
                {
                    title: 'agent.account.overview' | trans,
                    url: path('agent/overview'),
                    nodeType: 'category',
                    isActive: true,
                    validFrom: null,
                    validTo: null,
                    cssClass: null,
                    children: null
                },
                {
                    title: 'agent.account.quote_request' | trans,
                    url: url('agent/quote-request'),
                    nodeType: 'category',
                    isActive: true,
                    validFrom: null,
                    validTo: null,
                    cssClass: null,
                    children: null
                },
                {
                    title: 'agent.control_bar.logout' | trans,
                    url: path('agent_logout'),
                    nodeType: 'category',
                    isActive: true,
                    validFrom: null,
                    validTo: null,
                    cssClass: null,
                    children: null
                }
            ]
        }
    ]
} %}

{% block body %}
    {% include molecule('navigation-multilevel') with {
        data: {
            nodes: data.nodes,
            menuInline: true,
            menuDropdown: true
        }
    } only %}
{% endblock %}
