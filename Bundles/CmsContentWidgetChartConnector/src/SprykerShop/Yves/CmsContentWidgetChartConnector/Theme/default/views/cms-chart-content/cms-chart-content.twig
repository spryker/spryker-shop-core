{% extends template('widget') %}

{% define data = {
    chartPluginName: chartPluginName,
    dataIdentifier: dataIdentifier
} %}
{% block body %}
    {% if data.chartPluginName is not empty %}
        {% if widgetExists('ChartWidgetPlugin') %}
            {{ widget('ChartWidgetPlugin', data.chartPluginName, data.dataIdentifier) }} {# @deprecated Use NEW_MOLECULE instead. #}
        {% else %}
            {# todo: include NEW_MOLECULE here #}
        {% endif %}
    {% endif %}
{% endblock %}
