{% extends template('widget') %}

{% define data = {
    formObject: form,
} %}

{% block body %}
    {% for child in data.formObject.children %}
        <strong>{{ form_label(child) }}:</strong>
        {{ widget('ShoppingListItemProductOptionEditWidgetPlugin', child.vars.choices, child.vars.full_name) }}
        {{ form_errors(child) }}
    {% endfor %}
{% endblock %}
