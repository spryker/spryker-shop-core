{% extends model('component') %}

{% define config = {
    name: 'buttons-state-handler',
    tag: 'buttons-state-handler',
} %}

{% define attributes = {
    'trigger-class-name': required,
    'target-class-name': required,
} %}
