{% extends model('component') %}

{% define config = {
    name: 'buttons-disable-state-handler',
    tag: 'buttons-disable-state-handler',
} %}

{% define attributes = {
    'trigger-class-name': required,
    'target-class-name': required,
} %}
