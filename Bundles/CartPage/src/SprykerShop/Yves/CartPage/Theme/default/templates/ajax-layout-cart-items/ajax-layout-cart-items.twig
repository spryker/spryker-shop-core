{% define data = {
    cart: cart,
    cartItems: cartItems,
    isQuoteEditable: isQuoteEditable,
    attributes: attributes,
} %}

{% include molecule('product-cart-items', 'CartPage') with {
    data: {
        cart: data.cart,
        cartItems: data.cartItems,
        isQuoteEditable: data.isQuoteEditable,
        attributes: data.attributes,
    },
} only %}
