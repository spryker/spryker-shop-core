{% extends template('widget') %}

{% define data = {
    product: product | default,
    products: products | default([])
} %}

{% block body %}
    <div class="grid grid--stretch">
        {% for availableProduct in data.products %}
            {% widget 'CmsProductGroupWidget' args [availableProduct] only %}
            {% elsewidget 'ProductGroupWidgetPlugin' args [availableProduct] only %} {# @deprecated Use CmsProductGroupWidget instead. #}
            {% endwidget %}
        {% endfor %}
    </div>

    {% if data.product is not empty %}
        {% widget 'CmsProductGroupWidget' args [data.product] only %}
        {% elsewidget 'ProductGroupWidgetPlugin' args [data.product] only %} {# @deprecated Use CmsProductGroupWidget instead. #}
        {% endwidget %}
    {% endif %}
{% endblock %}
