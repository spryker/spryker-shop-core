Creates a block with configured bundle information: configured bundle name, configured bundle ID, configured bundle price, configured bundle quantity, configured bundle sum and list of included products.
