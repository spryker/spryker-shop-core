{% extends model('component') %}

{% define config = {
    name: 'company-business-unit-address-hundler',
    tag: 'company-business-unit-address-hundler'
} %}

{% define attributes = {
    'form-selector': required,
    'trigger-selector': required,
    'data-selector': required,
    'addresses': required
} %}
