{% extends template('widget') %}

{% define data = {
    isVisible: _widget.isVisible,
} %}

{% block body %}
    {% if data.isVisible %}
        <a href="{{ path('quote-request/cart/clear') }}" class="button button--secondary button--hollow">
            {{ 'quote_request_page.quote_request.actions.cancel' | trans }}
        </a>
    {% endif %}
{% endblock %}
